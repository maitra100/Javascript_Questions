const {func1,func2}=require('./q9');

test('replace every character in string with the following character',()=>{
    expect(
        func1("codeacademy")
    ).toBe("dpefbdbefnz")
}) 

test('replace every character in string with the following character',()=>{
    expect(
        func2("codeacademy")
    ).toBe("dpefbdbefnz")
}) 

test('replace every character in string with the following character',()=>{
    expect(
        func1("lazyinterns")
    ).toBe("mbazjoufsot")
}) 

test('replace every character in string with the following character',()=>{
    expect(
        func2("lazyinterns")
    ).toBe("mbazjoufsot")
}) 